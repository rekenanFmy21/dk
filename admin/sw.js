const CACHE_NAME = 'rekenan-admin-V200.82';
const BUILD = 'V200.82';
console.info('[Rekenan BUILD] SW', BUILD);
const STATIC = ['./', './index.html?v=2082', './manifest.json'];
self.addEventListener('install', event => { console.info('[Rekenan BUILD] SW V200.82 installing'); self.skipWaiting(); event.waitUntil(caches.open(CACHE_NAME).then(c => c.addAll(STATIC)).catch(() => {})); });
self.addEventListener('activate', event => { console.info('[Rekenan BUILD] SW V200.82 active'); event.waitUntil(caches.keys().then(keys => Promise.all(keys.filter(k => k !== CACHE_NAME).map(k => caches.delete(k)))).then(() => self.clients.claim())); });
self.addEventListener('message', event => { if (event.data && event.data.type === 'SKIP_WAITING') self.skipWaiting(); });
self.addEventListener('fetch', event => { if (event.request.method !== 'GET') return; const url = new URL(event.request.url); if (url.origin !== self.location.origin) return; const isHtml = event.request.mode === 'navigate' || url.pathname.endsWith('/index.html'); const isSw = url.pathname.endsWith('/sw.js'); if (isHtml || isSw) { event.respondWith(fetch(new Request(event.request, {cache:'no-store'})).then(resp => { const copy = resp.clone(); if (!isSw) caches.open(CACHE_NAME).then(c => c.put(event.request, copy)).catch(() => {}); return resp; }).catch(() => caches.match(event.request).then(r => r || caches.match('./index.html?v=2082')))); return; } event.respondWith(fetch(event.request).then(resp => { const copy = resp.clone(); caches.open(CACHE_NAME).then(c => c.put(event.request, copy)).catch(() => {}); return resp; }).catch(() => caches.match(event.request))); });
